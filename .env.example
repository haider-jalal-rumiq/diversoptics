# Phase 01 uses typed fictional fixtures only outside the Vercel production environment.
# Phase 02 will add the connected Supabase URL and publishable key by environment.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Vercel sets this automatically. Use "production" only for the real public destination.
VERCEL_ENV=development
